import Api = require('../api');
import Filters = require('./filters');
import Selects = require('./selects');
declare class QueryBuilder {
    build(selects: Selects.QuerySelects, filters: Filters.QueryFilter[], groups: string[], timeframe: Api.Timeframe, interval: string, timezone: Api.Timezone, customOptions: any): Api.Query;
    private _buildFilter(filters);
}
export = QueryBuilder;
