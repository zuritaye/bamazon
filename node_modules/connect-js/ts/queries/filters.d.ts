declare module Filters {
    class QueryFilter {
        field: string;
        operator: string;
        value: any;
        constructor(field: string, operator: string, value: any);
    }
    function queryFilterBuilder(filterValue: any, field: string): QueryFilter[];
}
export = Filters;
